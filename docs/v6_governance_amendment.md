# V6 governance amendment

- `test_iid` is classified as a corrected confirmatory holdout.
- The wrong-ladder protocol deviation, temporary-result hashes, exclusion, and non-selection evidence are bound in the JSON amendment.
- `matched-accuracy FVM` is replaced by `legal structured-FVM mesh sensitivity`.
- 4096/8192/16384 mean default/hotspot-oriented, balanced full-field, and maximum full-field accuracy.
- CPU→CPU and GPU→CPU speedups are distinct and nonmatched-DOF.
- Local CPU: Apple M4, 10 cores, 16 GB memory.
