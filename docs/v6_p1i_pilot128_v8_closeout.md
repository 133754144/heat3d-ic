# V6-P1i pilot128_v8 closeout

Status: **generated complete; qualification failed**.

The monotonic population rule passes every frozen gate except the 12-bin
occupancy ratio. The observed ratio is 5.333 rather than at most 5.0; core and
full gaps are 7.361 K and 7.583 K, 119/128 cases lie in 30--150 K, and no case
violates 20--180 K.

No sample was filtered or replaced. `formal1024_v1` remains closed. The next
attempt changes only the population-level monotonic curve and uses a new seed.
