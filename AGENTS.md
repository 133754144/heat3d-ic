# Heat3D-IC Agent Entry Rules

- Read `docs/codex_heat3d_workflow.md` before planning or editing.
- Keep this file short; put durable workflow rules in that document.
- For V4 work, stay on `research/v4` unless the user directs otherwise.
- Do not write to `data/`, `output/`, `checkpoints/`, or `logs/` without an
  explicit user request.
- Do not start training, add config generators, or add result-analysis scripts
  unless the user explicitly asks for that scope.
