# P23-R paired bootstrap

Status: **blocked fail-closed**.  The preregistered procedure remains 10,000
paired resamples of the 128 physical cases (seed `230023`), comparing the
case-level mean across the three frozen seeds as `Heat3D - Therm-FM`.

Heat3D V6 seed1 and seed2 checkpoints are missing, so no bootstrap was run and
no CI or superiority statement was produced.  See the JSON receipt for the
frozen protocol and explicit no-partial-inference flag.
