# V6-P1i pilot128_v7 closeout

Status: **generated complete; qualification failed**.

V7 passes every frozen gate except the 12-bin occupancy ratio. Its core gap is
4.306 K, its full gap is 8.919 K, and no case violates 20--180 K. However, the
140--150 K bin has one case while the largest bin has 21, so the frozen ratio
is 21.0 rather than at most 5.0.

No sample was filtered or replaced. `formal1024_v1` remains closed. The next
attempt replaces the linear severity mapping with one globally frozen monotonic
power-quantile curve calibrated from completed pilot populations; it does not
use per-sample Rth inversion or select samples after solving.
