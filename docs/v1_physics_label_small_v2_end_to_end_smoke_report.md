# Heat3D v1 Physics-Label Small v2 End-to-End Smoke Report

## Stage Purpose

This report records the current end-to-end smoke status for the Heat3D v1 physics-label small v2 path.

The current objective is to verify that the 16-sample v1 manifest can be generated with reference-solver-v2 label metadata, read by existing v1 supervised utilities, and evaluated by the existing zero-delta / train-valid / validation-metrics smoke route.

This is a label audit and v2-label smoke diagnostic. It is not a formal benchmark, not model-performance evidence, not OOD-generalization evidence, and not a high-fidelity solver validation.

## v2 Generation Summary

The v2 subset is:

`data/heat3d-thermal-simulation/subsets/v1_multilayer_bc_eq_physics_label_small_v2/`

It contains the full 16-sample manifest:

- train: 10 samples
- valid: 3 samples
- test_smoke: 1 sample
- test_ood_bc: 1 smoke candidate
- test_ood_stack: 1 smoke candidate

Each sample includes:

- metadata arrays from the v1 manifest-driven path
- `temperature.npy` generated by the minimal reference solver v2
- `label_meta.json` with solver metadata

The `test_ood_bc` and `test_ood_stack` samples remain diagnostic smoke candidates only. They are not OOD generalization evidence.

## Label Metadata And Diagnostics Summary

The label diagnostics smoke passed on the v2 subset:

- evaluated samples: 16
- `label_meta.json` present: 16 / 16
- diagnostics failures: 0
- bottom Dirichlet consistency: pass for all samples
- unsupported physics diagnostics: reported as not computed or requiring a numerical operator where appropriate

The solver metadata smoke summary from the label audit was:

- solver: `heat3d_v1_reference_solver_v2`
- solver version: `v2_minimal_fv_smoke_0`
- convergence flags: 16 true
- residual norm range: `5.77207494e-16` to `3.64332186e-15`
- mean residual norm: `2.19037192e-15`
- total solver warning count: 14

These values are smoke diagnostics for this controlled subset. They are not high-fidelity solver validation.

## Zero-Delta Bridge On v2 Labels

The zero-delta bridge smoke passed on the v2 subset.

The checked route remains:

- condition features: relative BC feature view
- legacy bridge: `zero_delta_u_bridge`
- target: `DeltaT = T - T_ref`
- recovery: `T_pred = T_ref + DeltaT_pred`

`label_meta.json` is not a model input. It is used only for diagnostics and reporting.

## Tiny Train-Valid Smoke On v2 Labels

The v2 validation metrics smoke was run with:

- steps: 20
- learning rate: `1e-5`
- seed: 0
- repeat runs: 2
- evaluated splits: train + valid
- checkpoint saved: false
- output file written: false

Observed smoke losses:

- train loss: `1.15936840e+00 -> 1.12858367e+00`
- valid loss: `8.57227564e-01 -> 8.33699048e-01`
- repeatability: pass

Recovered-temperature RMSE summaries:

- train zero-delta baseline mean RMSE: `4.11165429e-01`
- train tiny trained prediction mean RMSE: `3.25193623e-01`
- valid zero-delta baseline mean RMSE: `3.59073001e-01`
- valid tiny trained prediction mean RMSE: `2.94390613e-01`

This indicates that the train/valid smoke loop can consume v2 labels and produce finite, repeatable diagnostic metrics. It is not a model-performance claim.

## Validation Metrics Smoke On v2 Labels

The validation metrics smoke produced:

- per-sample metric rows: 26
- split summaries: 4
- label metadata files in evaluated samples: 13 / 13
- gradient finite check: true
- validation metrics smoke ok: true

Covered diagnostic metrics include:

- raw `DeltaT` MSE
- recovered-temperature RMSE / MAE / max absolute error
- peak-temperature absolute error
- hotspot index and coordinate distance
- top-k hotspot overlap
- split-level summaries

These metrics are used to verify the evaluation interface and label path. They are not formal benchmark metrics yet.

## Legacy Default Smoke Status

The legacy default supervised-small smoke path still passes after adding v2 subset support. The default path continues to read:

`data/heat3d-thermal-simulation/subsets/v1_multilayer_bc_eq_supervised_small/`

The v2 path is additive and does not overwrite the legacy supervised-small subset.

## Smoke-vs-v2 Label Comparison

The smoke-vs-v2 label audit compared the legacy supervised-small labels against the v2 labels for all 16 common sample ids.

Summary:

- common samples: 16
- mean label-difference RMSE: `0.00000000e+00`
- mean label-difference MAE: `0.00000000e+00`
- max absolute label difference: `0.00000000e+00`
- mean absolute peak-temperature difference: `0.00000000e+00`
- hotspot index matches: 16 / 16
- v2 `label_meta.json` present: 16 / 16

This means the current v2 generation path reproduces the existing smoke labels for this controlled 16-sample subset while adding explicit solver metadata. It does not mean the labels are high fidelity, independently validated, or suitable as a formal benchmark.

## Non-Claims

Do not claim from this stage:

- high-fidelity solver validation
- formal benchmark readiness
- model performance
- OOD generalization
- industrial 3D IC package simulation
- COMSOL/FEM equivalence
- completed physics residual / flux / energy validation

The current v2 subset is a research reference and benchmark-candidate smoke dataset.

## Next Recommended Step

The next stage should define a baseline/model comparison protocol before expanding training claims.

Recommended focus:

- freeze the v2 label audit outputs as diagnostic reference checks
- decide which splits are allowed for training, validation, and diagnostic-only reporting
- define baseline predictors and reporting tables
- keep `test_ood_bc` and `test_ood_stack` clearly marked as smoke candidates
- only later consider broader solver fidelity improvements or larger datasets
